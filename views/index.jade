extends layout

block content
  h1= title
  p #{app} is your spot for voter information available through public sources.  We believe that information being provided by our government and used by political organizations should be just as available to citizens.
  #search
    label Voter:
      input(name="voter", id="voter", autofocus)

  ul#voters

block scripts
  != js('jquery')
  != js('index')
